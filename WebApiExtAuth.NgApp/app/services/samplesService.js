'use strict';
app.factory('samplesService', ['$http', '$q', 'localStorageService', function ($http, $q, localStorageService) {

  var serviceBase = 'http://localhost:63345/';
  var samplesServiceFactory = {};

  return samplesServiceFactory;
}]);